# Get nice terminal prompt and enable colors
set-option -g default-command bash
set -g default-terminal "screen-256color"
# Add prefix of a ` (back-dash)
set-option -g prefix `
bind ` send-prefix
# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1